spring:
  config:
    activate:
      on-profile: production
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgresql://name99_user:L2dTkyNe9PJCDz5RYNgm6DMOGxcSB2gT@dpg-d43fp1ali9vc73cvoaf0-a.oregon-postgres.render.com/name99}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:password}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
  output:
    ansi:
      enabled: never